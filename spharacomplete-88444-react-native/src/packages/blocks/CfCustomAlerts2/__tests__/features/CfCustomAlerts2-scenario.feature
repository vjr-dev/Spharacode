Feature: CfCustomAlerts2

    Scenario: User navigates to CfCustomAlerts2
        Given I am a User loading CfCustomAlerts2
        When I navigate to the CfCustomAlerts2
        Then CfCustomAlerts2 will load with out errors
        And I can enter text with out errors
        And I can select the button with with out errors
        And I can leave the screen with out errors