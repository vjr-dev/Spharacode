import React from "react";

import {
  // Customizable Area Start
  View,
  StyleSheet
  // Customizable Area End
} from "react-native";

import OnlineBackupController, {
  Props,
  configJSON
} from "./OnlineBackupController";

// Customizable Area Start
// Customizable Area End

export default class OnlineBackup extends OnlineBackupController {
  constructor(props: Props) {
    super(props);
    // Customizable Area Start
    // Customizable Area End
  }

  // Customizable Area Start
  // Customizable Area End

  render() {
    return (
      // Customizable Area Start
      <View></View>
      // Customizable Area End
    );
  }
}

// Customizable Area Start
const styles = StyleSheet.create({

});
// Customizable Area End
