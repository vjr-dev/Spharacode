import React from "react";

import {
  // Customizable Area Start
  StyleSheet,
  View,
  // Customizable Area End
} from "react-native";

import ConversationThreadingController, {
  Props,
  configJSON
} from "./ConversationThreadingController";
// Customizable Area Start
// Customizable Area End

export default class ConversationThreading extends ConversationThreadingController {
   
  constructor(props: Props) {
    super(props);
    // Customizable Area Start
    // Customizable Area End
  }

  // Customizable Area Start
  // Customizable Area End

  render() {
    return (
      // Customizable Area Start
     <View>

     </View>
      // Customizable Area End
    );
  }
}

// Customizable Area Start
const styles = StyleSheet.create({
 
});
// Customizable Area End
